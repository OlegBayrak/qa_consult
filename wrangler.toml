# Cloudflare Pages / Workers — static site deploy
# Build runs first (npm run build), then wrangler deploys dist/
name = "qa-consult"
compatibility_date = "2026-02-18"

[assets]
directory = "./dist"
