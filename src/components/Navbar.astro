---
import ThemeToggle from './ThemeToggle.astro';

const links = [
  { label: 'About', href: '#about' },
  { label: 'Skills', href: '#skills' },
  { label: 'Portfolio', href: '#portfolio' },
  { label: 'Experience', href: '#experience' },
  { label: 'Contact', href: '#contact' },
];
---

<nav id="navbar" class="fixed top-0 w-full z-50 transition-all duration-300">
  <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
    <a href="#" class="font-bold text-lg text-white dark:text-white hover:text-accent transition-colors">
      <span class="font-mono text-accent">&lt;</span><span class="text-white dark:text-white">OB</span><span class="font-mono text-accent">/&gt;</span>
    </a>

    <!-- Desktop nav -->
    <div class="hidden md:flex items-center gap-8">
      {links.map((link) => (
        <a
          href={link.href}
          class="nav-link text-sm text-gray-400 dark:text-gray-400 hover:text-accent transition-colors duration-200 font-medium"
        >
          {link.label}
        </a>
      ))}
      <ThemeToggle />
    </div>

    <!-- Mobile: theme toggle + hamburger -->
    <div class="flex md:hidden items-center gap-2">
      <ThemeToggle />
      <button id="mobile-toggle" class="text-gray-400 hover:text-white dark:hover:text-white transition-colors" aria-label="Toggle menu">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path id="hamburger-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="md:hidden hidden backdrop-blur-lg border-t
    dark:bg-surface/95 dark:border-white/5
    bg-white/95 border-slate-200/60">
    <div class="px-6 py-4 flex flex-col gap-4">
      {links.map((link) => (
        <a
          href={link.href}
          class="mobile-link text-gray-400 dark:text-gray-400 hover:text-accent transition-colors font-medium"
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>
</nav>

<script>
  const navbar = document.getElementById('navbar')!;
  const mobileToggle = document.getElementById('mobile-toggle')!;
  const menu = document.getElementById('mobile-menu')!;

  // Scroll effect — adapts to dark/light
  function updateNavbarScroll() {
    const isDark = document.documentElement.classList.contains('dark');
    if (window.scrollY > 50) {
      if (isDark) {
        navbar.classList.add('bg-primary/90', 'backdrop-blur-lg', 'shadow-lg', 'shadow-black/10');
        navbar.classList.remove('bg-white/90', 'shadow-slate-200/50');
      } else {
        navbar.classList.add('bg-white/90', 'backdrop-blur-lg', 'shadow-lg', 'shadow-slate-200/50');
        navbar.classList.remove('bg-primary/90', 'shadow-black/10');
      }
    } else {
      navbar.classList.remove('bg-primary/90', 'bg-white/90', 'backdrop-blur-lg', 'shadow-lg', 'shadow-black/10', 'shadow-slate-200/50');
    }
  }

  window.addEventListener('scroll', updateNavbarScroll);

  // Re-apply on theme change
  const themeToggleBtn = document.getElementById('theme-toggle');
  if (themeToggleBtn) {
    themeToggleBtn.addEventListener('click', () => {
      setTimeout(updateNavbarScroll, 50);
    });
  }

  // Mobile toggle
  mobileToggle.addEventListener('click', () => {
    menu.classList.toggle('hidden');
  });

  // Close mobile menu on link click
  document.querySelectorAll('.mobile-link').forEach((link) => {
    link.addEventListener('click', () => menu.classList.add('hidden'));
  });
</script>

<style>
  /* Light mode nav link colors */
  html:not(.dark) .nav-link {
    @apply text-slate-500;
  }
  html:not(.dark) .nav-link:hover {
    @apply text-accent;
  }
  html:not(.dark) .mobile-link {
    @apply text-slate-600;
  }
  /* Light mode logo text */
  html:not(.dark) nav a span:not(.font-mono) {
    color: #0f172a;
  }
</style>
